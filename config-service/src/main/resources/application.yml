server:
  port: 8083
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          default-label: multiple-instances
          uri: https://github.com/alejorcode/microservices-1
          search-paths: config-data
          clone-on-start: true
